# Pinned to a tested-good environment (Python 3.13 — see .python-version).
# The app is 100% canvas now: matplotlib and plotly are no longer imported by
# any page (they remain dev/notebook dependencies only, not deploy ones).
numpy==2.4.6
pandas==3.0.3
streamlit==1.58.0
Pillow==12.2.0
